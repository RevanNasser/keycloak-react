# Keycloak Configuration
# Copy this file to .env.local for local development
# Note: Next.js requires NEXT_PUBLIC_ prefix for client-side environment variables

# Keycloak Server URL
# The base URL where your Keycloak server is running
# Default: http://localhost:8080
NEXT_PUBLIC_KEYCLOAK_URL=http://localhost:8080

# Keycloak Realm
# The realm name in Keycloak where your client is configured
# Default: master (use a custom realm for production)
NEXT_PUBLIC_KEYCLOAK_REALM=master

# Keycloak Client ID
# The client ID that matches the client you created in Keycloak
# This must exactly match the Client ID in Keycloak admin console
# Default: react-client
NEXT_PUBLIC_KEYCLOAK_CLIENT_ID=react-client
